name: Snap Testing

on:
  pull_request:
    branches: [ main ]
  # allow manual trigger
  workflow_dispatch:

jobs:
  snap-testing:
    uses: canonical/edgex-snap-testing/.github/workflows/build+test.yml@reusable-workflow
    with:
      name: device-mqtt
  