name: Snap Testing

on:
  pull_request:
    branches: [ main ]
  # allow manual trigger
  workflow_dispatch:

jobs:
  build:
    name: Build Snap
    runs-on: ubuntu-latest
    steps:
      - name: Build and upload snap
        id: build
        uses: canonical/edgex-snap-testing/build@v1
    outputs:
      snap: ${{steps.build.outputs.snap}}

  test:
    name: Test Snap
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Download and test snap
        uses: canonical/edgex-snap-testing/test@v1
        with:
          name: device-mqtt
          snap: ${{needs.build.outputs.snap}}
